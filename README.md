## Spustit
Zkopírovat soubor /api/config-example.php do /api/config.php a doplnit údaje

spustit: docker compose up -d

pak dostupné na http://localhost:8080/api/login.php

## Odstranit
docker compose down -v